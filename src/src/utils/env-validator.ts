/**
 * Validación de variables de entorno al inicio de la aplicación
 * Re-exporta desde la ubicación canónica en src/utils/env-validator
 */
export { validateEnv, assertEnv } from '../../utils/env-validator';
export type { EnvValidationResult } from '../../utils/env-validator';
export { default } from '../../utils/env-validator';
